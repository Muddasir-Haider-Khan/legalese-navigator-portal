
// This component is kept but not used in the UI
// It's maintained for potential future use if needed

import { Moon } from "lucide-react";
import { Button } from "@/components/ui/button";
import { memo } from "react";

export const ThemeToggle = memo(() => {
  return null; // Not rendering anything as the component is not used
});

ThemeToggle.displayName = 'ThemeToggle';

export const ThemeToggleMinimal = memo(() => {
  return null; // Not rendering anything as the component is not used
});

ThemeToggleMinimal.displayName = 'ThemeToggleMinimal';
